import 'package:flutter/material.dart';

class HomePageWithGlobalData extends StatelessWidget {
  const HomePageWithGlobalData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
